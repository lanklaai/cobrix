[package]
name = "cobrix-rust"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Spark-free COBOL copybook parser and fixed-length record reader inspired by Cobrix"

[dependencies]
thiserror = "1"

[dev-dependencies]
pretty_assertions = "1"
